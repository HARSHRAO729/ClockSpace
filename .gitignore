# Xcode
#
build/
DerivedData/
*.hmap
*.ipa
*.playground.xcworkspace
xcuserdata/

# Swift Package Manager
#
.build/
.swiftpm/

# OS X
#
.DS_Store
*.swp
*.lock

# ClockSpace Specific
/BundledSavers/
Secrets.xcconfig
GoogleService-Info.plist
ClockSpaceApp/Resources/temp_migration/savers/
# (Wait, if I want the 54 launchers to be saved, I SHOULD push them)
# Actually, the user wants the 54 files in ClockSpaceApp/Resources/BundledSavers/ to be included.
# So I won't ignore those.
*.dmg

# Local staging for Cloudflare R2 asset migration (not committed)
r2-upload/

# Built saver bundles — rebuilt from source / uploaded to R2, never committed
dist/

# Python
__pycache__/
